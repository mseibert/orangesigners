---
// Diese Seite wird bei fehlender Authentifizierung angezeigt
---

<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zugriff erforderlich - Orange Singers</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Atkinson Hyperlegible', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #333;
        }

        .container {
            background: white;
            border-radius: 20px;
            padding: 3rem;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 500px;
            margin: 2rem;
            position: relative;
            overflow: hidden;
        }

        .container::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.1), transparent);
            animation: shimmer 3s infinite;
        }

        @keyframes shimmer {
            0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
            100% { transform: translateX(100%) translateY(100%) rotate(45deg); }
        }

        .octopus {
            width: 120px;
            height: 120px;
            margin: 0 auto 2rem;
            position: relative;
            z-index: 1;
        }

        .octopus-head {
            width: 80px;
            height: 80px;
            background: #FF6B6B;
            border-radius: 50%;
            margin: 0 auto;
            position: relative;
            box-shadow: 0 8px 16px rgba(255, 107, 107, 0.3);
        }

        .octopus-eye {
            width: 16px;
            height: 16px;
            background: #333;
            border-radius: 50%;
            position: absolute;
            top: 25px;
            animation: blink 4s infinite;
        }

        .octopus-eye.left {
            left: 20px;
        }

        .octopus-eye.right {
            right: 20px;
        }

        @keyframes blink {
            0%, 90%, 100% { transform: scaleY(1); }
            95% { transform: scaleY(0.1); }
        }

        .octopus-mouth {
            width: 20px;
            height: 12px;
            border: 3px solid #333;
            border-top: none;
            border-radius: 0 0 20px 20px;
            position: absolute;
            bottom: 15px;
            left: 50%;
            transform: translateX(-50%);
        }

        .octopus-tentacle {
            width: 8px;
            height: 40px;
            background: #FF6B6B;
            border-radius: 4px;
            position: absolute;
            bottom: -20px;
            animation: wave 2s ease-in-out infinite;
        }

        .octopus-tentacle:nth-child(1) { left: 10px; animation-delay: 0s; }
        .octopus-tentacle:nth-child(2) { left: 20px; animation-delay: 0.2s; }
        .octopus-tentacle:nth-child(3) { left: 30px; animation-delay: 0.4s; }
        .octopus-tentacle:nth-child(4) { left: 40px; animation-delay: 0.6s; }
        .octopus-tentacle:nth-child(5) { right: 40px; animation-delay: 0.8s; }
        .octopus-tentacle:nth-child(6) { right: 30px; animation-delay: 1s; }
        .octopus-tentacle:nth-child(7) { right: 20px; animation-delay: 1.2s; }
        .octopus-tentacle:nth-child(8) { right: 10px; animation-delay: 1.4s; }

        @keyframes wave {
            0%, 100% { transform: rotate(0deg); }
            50% { transform: rotate(10deg); }
        }

        h1 {
            color: #333;
            margin-bottom: 1rem;
            font-size: 2rem;
            font-weight: bold;
        }

        .message {
            color: #666;
            margin-bottom: 2rem;
            line-height: 1.6;
            font-size: 1.1rem;
        }

        .contact-info {
            background: #f8f9fa;
            border-radius: 12px;
            padding: 1.5rem;
            margin-bottom: 2rem;
            border-left: 4px solid #FF6B6B;
        }

        .email {
            color: #FF6B6B;
            text-decoration: none;
            font-weight: bold;
            font-size: 1.1rem;
        }

        .email:hover {
            text-decoration: underline;
        }

        .bubbles {
            position: absolute;
            width: 100%;
            height: 100%;
            overflow: hidden;
            pointer-events: none;
        }

        .bubble {
            position: absolute;
            background: rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            animation: float 6s ease-in-out infinite;
        }

        .bubble:nth-child(1) {
            width: 20px;
            height: 20px;
            left: 10%;
            animation-delay: 0s;
        }

        .bubble:nth-child(2) {
            width: 15px;
            height: 15px;
            left: 20%;
            animation-delay: 2s;
        }

        .bubble:nth-child(3) {
            width: 25px;
            height: 25px;
            left: 70%;
            animation-delay: 4s;
        }

        .bubble:nth-child(4) {
            width: 18px;
            height: 18px;
            left: 40%;
            animation-delay: 1s;
        }

        .bubble:nth-child(5) {
            width: 12px;
            height: 12px;
            left: 60%;
            animation-delay: 3s;
        }

        .bubble:nth-child(6) {
            width: 22px;
            height: 22px;
            left: 80%;
            animation-delay: 5s;
        }

        .bubble:nth-child(7) {
            width: 16px;
            height: 16px;
            left: 30%;
            animation-delay: 1.5s;
        }

        .bubble:nth-child(8) {
            width: 14px;
            height: 14px;
            left: 50%;
            animation-delay: 2.5s;
        }

        .bubble:nth-child(9) {
            width: 19px;
            height: 19px;
            left: 90%;
            animation-delay: 3.5s;
        }

        .bubble:nth-child(10) {
            width: 13px;
            height: 13px;
            left: 15%;
            animation-delay: 4.5s;
        }

        @keyframes float {
            0%, 100% { transform: translateY(100vh) scale(0); opacity: 0; }
            10% { opacity: 1; }
            90% { opacity: 1; }
            100% { transform: translateY(-100px) scale(1); opacity: 0; }
        }

        .content {
            position: relative;
            z-index: 2;
        }
    </style>
</head>
<body>
    <div class="bubbles">
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
    </div>
    
    <div class="container">
        <div class="content">
            <div class="octopus">
                <div class="octopus-head">
                    <div class="octopus-eye left"></div>
                    <div class="octopus-eye right"></div>
                    <div class="octopus-mouth"></div>
                </div>
                <div class="octopus-tentacle"></div>
                <div class="octopus-tentacle"></div>
                <div class="octopus-tentacle"></div>
                <div class="octopus-tentacle"></div>
                <div class="octopus-tentacle"></div>
                <div class="octopus-tentacle"></div>
                <div class="octopus-tentacle"></div>
                <div class="octopus-tentacle"></div>
            </div>

            <h1>👋 Hallo!</h1>
            
            <div class="message">
                Diese Seite ist derzeit noch passwortgeschützt. 
                Unser freundlicher Krake winkt dir zu und freut sich darauf, 
                dich bald auf der Orange Singers Website begrüßen zu dürfen!
            </div>

            <div class="contact-info">
                <p><strong>Zugriff benötigt?</strong></p>
                <p>Kontaktiere uns gerne:</p>
                <a href="mailto:juliane.seibert2@schule.hessen.de" class="email">juliane.seibert2@schule.hessen.de</a>
            </div>
        </div>
    </div>
</body>
</html>
